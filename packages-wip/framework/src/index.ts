/*import VeridaDatabase from '@verida/database'

class Verida {

    constructor(config) {
        // set resolvers {vid: vidResolver(), 3id: 3idResolver()}
        this.resolvers = config.resolvers

        // set environment (testnet, mainnet)
        this.environment = config.environment
    }

    // return new DID
    async openDID(config) {}

    // open external database
    async openExternalDatabase(config) {
        const db = new VeridaDatabase()
    }

    // open external datastore
    async openExternalDatastore(config) {}

    // open external profile
    async openProfile(config) {}

    async getSchema() {}
    
    async buildDataserver() {}

    // resolve a VID DID document or 3ID
    async resolve(did) {}

}

export default Verida*/