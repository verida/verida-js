
// verida or 3id DID for a given application
class DID {

    constructor(config) {
        // entropy
        // did (string) -> chain + address
        // keyring

        // 3ID v Verida customisation:
        // signer (function to sign messages by this DID)
    }

    signData(data) {

    }

    // Set the dataserver dsn for this DID, app name
    setDataserver(context, dsn) {}

    // Get the dataserver for this DID and context (app name)
    getDataserver(context) {}



}