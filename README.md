

Getting started
------------------

In the root folder, run:

```
yarn install
npx lerna link
cd storage
yarn install
yarn run build
cd ../storage-connection-ethr
yarn install
yarn run build
yarn run test
```